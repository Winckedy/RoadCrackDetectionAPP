<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_location_status"
        android:title="@string/location_status_title"
        android:icon="@drawable/ic_location_off"
    android:contentDescription="@string/location_status_description"
    app:showAsAction="always" />
    <!-- 保留你原有的历史记录按钮 -->
    <item
        android:id="@+id/action_history"
        android:title="@string/history_title"
        android:icon="@drawable/ic_history"
        android:contentDescription="@string/history_button_description"
        app:showAsAction="ifRoom" />

</menu>

